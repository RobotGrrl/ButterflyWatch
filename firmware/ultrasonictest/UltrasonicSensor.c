#include "UltrasonicSensor.h"
#include "Board.h"

void updateUltrasonicSensor(struct UltrasonicSensor *s) {

    // only update if everything's ready
    if(sen->active == false && sen->ready == false) return;

    

}